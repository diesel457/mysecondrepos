index(bem='Input' attributes='label description')
  view(is='{{ _layout }}' inherit)

pure:
  view.-pure(is='input' extraClass='{{ @class }}' inherit)

columns:(bem='Input')
  .&.-columns.-textarea(class='{{ @class }}')
    .&-left
      label.&-label {{ @label }}
    .&-right
      view(is='input' inherit)
      if @description
        .&-description {{ @description }}

input:(bem='Input')
  if @readonly
    view(is='input:readonly' inherit)
  else
    view(is='input:edit' inherit)

input:readonly:(bem='Input')
  .&-input.-textarea.-readonly(class='{{ @extraClass }}')
    | {{ _value || '&nbsp;' }}

input:edit:(bem='Input')
  textarea.&-input.-textarea(class='{{ @extraClass }}'
      disabled='{{ @disabled }}'
      placeholder="{{ @placeholder || 'write here' }}"
      on-blur='lazySet()') {{ _value }}
